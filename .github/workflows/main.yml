name: Basic Commands1

on: [push]
jobs:
  demo-commands1:
    runs-on: ubuntu-latest

    steps: 
      - name: Show current working directory 
        run: pwd

      - name: List files in the directory
        run: ls -la
